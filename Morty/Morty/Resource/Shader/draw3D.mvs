#include "privateHeader.hlsl"

struct VS_OUT
{
    float4 pos : SV_POSITION;
    float4 color : COLOR;
};

VS_OUT VS(VS_IN_COLOR input)
{
    VS_OUT output;

    output.pos = mul(float4(input.pos, 1.0f), U_matCamProj);
    output.color = input.color;

    return output;
}